# Only needed for the opt-in "high-accuracy" paths (Section 4.4.1 Tier 3,
# 4.4.2 paid path, and the AI QA pass). Free-tier extraction and dimension
# suggestion work without this.
#
# Local dev:  wrangler secret put ANTHROPIC_API_KEY --local
# Production: wrangler secret put ANTHROPIC_API_KEY
ANTHROPIC_API_KEY=
